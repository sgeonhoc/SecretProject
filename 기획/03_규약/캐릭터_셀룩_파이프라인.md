# 🎨 캐릭터 셀룩(Hanekawa풍) 파이프라인 — 단일 출처 (SSOT)

> **세션이 끊겨도 이 파일만 읽으면 목적·논리·진행상황·실행법이 전부 복원된다.**
> 최초 정리: 2026-06-05 (A가 B의 마지막 세션 `a146d6a6` 복기해 작성). 이 워크스트림 = "우리 VRoid 캐릭터에 Hanekawa 셀룩 입히기".

---

## 🎯 진짜 목적 (사용자 원문 의도)
**우리 VRoid 캐릭터를 Hanekawa(CiciToon 데모 모델)의 고퀄 셀룩(셀 음영·또렷한 눈동자·외곽선)으로 바꾸되, VRoid 리깅(뼈대·블렌드셰이프)은 그대로 둬서 VRM 기능을 100% 살린다.**

## ⛔ 왜 Tripo는 안 쓰는가 (폐기된 접근 — 다시 헷갈리지 말 것)
- 처음엔 Tripo(이미지→3D 재생성)로 시도했으나 얼굴이 뭉개짐. 원인 = Tripo 한계 아님, **입력이 쓰레기**였음(기성 3D를 Blender 플랫 렌더한 저해상·무음영·T포즈 = GIGO).
- 그 후 사용자 아이디어 "Tripo에 리깅된 VRoid 올려 겉모습만 변환, 뼈대 유지"도 **B가 검증 결과 불가능**으로 판명:
  - `stylize_model` = lego/voxel/minecraft 프리셋만 (Hanekawa풍 ❌)
  - `texture_model` = Tripo 자체생성 모델(task_id)에만 + **VRoid 뼈대·블렌드셰이프 보존 안 됨**
  - **Tripo는 항상 자기 메시·자기 리그를 새로 만듦 → VRoid 리깅 소멸.**
- ⚠️ `_tripo/`, `tripo_pipeline.py`, `_tripo/README.txt` = **그 시절 잔재**. 현재 계획에선 Tripo 사용 안 함. (헷갈림 주의)

## ✅ 현재 확정된 길 (Tripo 불필요, 사용자 합의 완료)
1. **우리 VRoid 캐릭 파츠별 + 각도별 촬영** (Hanekawa 찍은 그 방법 그대로, 셀 렌더 살아있게)
2. **AI 이미지 변환(외부 img2img 사이트)** → 우리 캐릭 사진을 **Hanekawa 스타일**로 변환
3. ★ **그 Hanekawa풍 이미지로 VRoid의 텍스처 아틀라스(얼굴/눈/머리/피부/옷)를 다시 칠하기**
   → 메시·뼈대·블렌드셰이프 **0 변경 = VRM 기능 그대로**, 겉(텍스처/셰이더)만 Hanekawa풍.
   - test7은 텍스처 아틀라스(`T__01~19`, Face/Hair 맵) 그대로 있어 이 경로에 적합 확인됨.

## 📌 진행 상황 (where we are)
- ✅ **Hanekawa 레퍼런스 캡처 완료**: `_tripo/hayakawa_caps/full/` **37장** (셀 음영 살아있는 진짜 퀄. 벤치/방법 확립).
- ✅ **test7 프로브 4장 완료**: `_tripo/our_chars/test7_probe/` — **정면 = FRONT_YAW 0 확정**. 메이드복 VRoid 여캐.
- ✅ **test7 full 37장 완료**(06-05, A): `_tripo/our_chars/test7/`. 보정 1회 거침 — eyes/hand/shoes 빗나간 것 수정(눈 본 `J_Adj_L_FaceEye` z=161.6 앵커, head 본 155.9는 입높이라 폐기 / 손 look_x 바깥 +7 / 신발 줌아웃). **eyes·hand·shoes·face_front·전신8각 = 굿. nose/mouth 미세샷만 살짝 낮음(B 기준 "충분").**
- ⏭ **다음 = test7을 AI img2img로 Hanekawa 스타일 변환** → VRoid 텍스처 아틀라스(`T__01~19`/Face/Hair) 교체 → 나머지 캐릭(SK_test1~23) 반복(각자 프로브→full).
- 📎 **재캡처 실행법(중요, 내가 피흘려 알아냄):** 반드시 **PowerShell + `&` 호출연산자 + `--%` + `-unattended`** 로 띄워야 슬레이트 틱이 돌아 캡처됨. `Start-Process`/`-ExecutePythonScript`/Git Bash로는 워밍업에서 멈춤. 명령: `& "<UE>\UnrealEditor.exe" --% "<.uproject>" -ExecCmds="py C:/Secret_Project/capture_character.py" -nosplash -unattended -nopause`

## 🛠️ 캡처 실행법 (재현 가능)
- 스크립트: `C:\Secret_Project\capture_character.py` (범용 VRoid 파츠 캡처. CONFIG 상단 `MESH_PATH/CHAR/FRONT_YAW/MODE`만 바꿈).
  - test7: `MESH_PATH=/Game/TestCharacter/test7/SK_test7`, `CHAR=test7`, `FRONT_YAW=0`, `MODE="full"`(probe로 방향확정 후).
- 깨끗한 배경·조명 = **`/Game/CiciToonCharacterShaderPak/Levels/Map_Main`** (스크립트가 직접 load_level). 새 조명 짜지 말 것(검게 크러시 위험).
- **★실행은 반드시 PowerShell로** (Git Bash는 경로/따옴표 망가뜨림 — 사용자 지시). 명령:
  ```
  & "C:\Program Files\Epic Games\UE_5.7\Engine\Binaries\Win64\UnrealEditor.exe" "C:\Secret_Project\Secret_Project.uproject" -ExecCmds="py C:/Secret_Project/capture_character.py" -nosplash -nopause
  ```
  또는 에디터 열고 Output Log → 콘솔 드롭다운 Python → `py "C:/Secret_Project/capture_character.py"`.
- 진행 로그: `C:\Secret_Project\../99_보관/구로그/citoon_capture_log.txt` (warmup done → SHOT 00.. → DONE). 결과 PNG는 OUT 폴더로 자동 이동, 끝나면 QUIT_EDITOR.

## ⚠️ 캡처 함정 (B가 피 흘려 알아낸 것 — 반복 금지)
- **렌더는 SceneCapture2D 금지** → CiciToon 외곽선/셀이 메인뷰 포스트프로세스 의존 → 검게 나옴. **실제 뷰포트 + `take_high_res_screenshot`(카메라 FOV/세로해상도 제어)** 사용.
- **화질**: `r.Streaming.FullyLoadUsedTextures 1` + **워밍업 대기**(420틱) 안 하면 텍스처 안 깔려 얼굴 흐림.
- **출력 비율**: HighResShot이 요청 해상도 무시하고 **에디터 창 16:9 가로로 고정** → 프레이밍은 16:9 세로화각 기준 계산.
- **포즈**: 레퍼런스(A/바인드)포즈 강제(손 내림). 손이 얼굴 가리면 레퍼런스 못 씀.
- **얼굴/손 클로즈업은 본(뼈) 좌표 앵커링**: head bone z≈141(눈높이), R_Hand/L_Hand 좌표 사용. 추측 z(155)는 정수리 잡힘.
- **틱 의존**: `register_slate_post_tick_callback` 기반이라 **명령줄 실행해도 에디터가 떠서 메인루프가 돌아야** 워밍업/샷 진행됨. Bash로 띄우면 틱이 안 돌던 사례 있음 → **PowerShell 사용**.

## 🧪 텍스처교체 검증 (06-05, test7) — ★중요 현실점검
- **test7 아틀라스 추출 완료**: `_tripo/our_chars/test7_tex/` (`export_char_tex.py`로 재현). 맵:
  - `T__04`=얼굴피부 / `T__10`=몸피부 / `T__13`=의상(메이드드레스) / `T__12`=머리(갈색) / `T__02`=눈동자(파랑) / `T__17`=프릴트림 / `T__11`=노멀맵(제외).
- ⚠️ **원안의 약점(반드시 인지):**
  1. 이건 **UV 아틀라스**라 img2img가 레이아웃을 이해 못 함. 몸/머리/드레스는 펼친 조각이라 그대로 변환하면 **UV 깨짐**. 얼굴(T__04)만 반정면이라 그나마 가능.
  2. **test7은 이미 MToon 셀셰이더+`__outline`** = 이미 셀룩. Hanekawa와 격차는 "셀 부재"가 아니라 **아트 퀄(텍스처 디테일)**. (B 노트 "셀룩=툰셰이더 전략"과 일치)
- **검증 절차:** ①사용자가 `T__04.png`를 img2img(레퍼=hayakawa face_front, **denoise 0.3~0.45, 해상도·레이아웃 보존**)로 Hanekawa화 → `T__04_styled.png` → ②A가 원본 백업 후 얼굴 머티리얼에 재임포트 → ③안 깨지고 셀룩 좋아지고 블렌드셰이프 유지되면 **유효** → 몸/머리/드레스 확장. 깨지면 피벗(스크린샷 재투영 / 셰이더 튜닝 / 수동 보정).
- **재임포트 시 원본 보존 필수**(롤백 가능하게). img2img는 외부사이트=사용자 전용, 재임포트·검증=A.

## 🧩 그리드 배치 툴 (img2img 한 방에 올리기) — `grid_tool.py`
파츠가 많아 한 장씩 못 올릴 때, 여러 파츠를 통짜 그리드 1장으로 합쳐 SeaArt 등 img2img에 한 번에 올리고, 결과를 좌표로 다시 분할. (Pillow는 `_pylibs`에 설치됨. UE 번들 python으로 실행.)
- **합치기:** `"<UE>\...\python.exe" C:\Secret_Project\grid_tool.py pack --in <폴더> [--cell 512] [--pad 0]` → `_tripo/grids/<>_grid.png` + `.json`(좌표). 원본비율 보존·알파 보존·셸프패킹.
- **분할(AI 처리 후):** `... grid_tool.py split --grid <AI결과.png> --json <그 json> --out <복원폴더>` (텍스처면 `--restore-orig` 추가=원본해상도). AI가 전체 크기 바꿔도 좌표 비율로 보정.
- test7 렌더 37장 → `_tripo/grids/test7_grid.png`(3072x2016) 생성됨. ※렌더 대신 텍스처 아틀라스를 그리드하려면 `--in ..._tex --cell 0`, split 시 `--restore-orig`.

## ✅ 작동 확정 레시피 (06-05, test7 검증완료)
1. **정사각 그리드 사용**: `test7_tex_grid_sq.png`(3112², 파츠 좌상단+회색 패딩) + `_sq.json`. (3:2 그리드를 1:1로 출력하면 비율 불일치로 split 깨짐 — 반드시 입력=출력 비율.)
2. **SeaArt: 1:1 출력** (정사각 입력↔정사각 출력 = 균일 스케일 = 깨끗). denoise 0.45~0.55(외곽선).
3. **split**: `grid_tool.py split --grid <결과> --json _sq.json --out <styledN> --restore-orig` (알파 자동 보존).
4. **재임포트**: `reimport_tex.py`(SRC만 styledN로 바꿈) 헤드리스 `-Cmd -ExecutePythonScript`. 원본 .uasset 백업 = `test7_tex_backup_uasset`(롤백용).
5. **확인**: ⚠️ **37장 캡처(렌더) 금지 — 낭비(사용자 지시).** 재임포트 후 `open_test7_view.py`로 **라이브 에디터**만 띄워 사용자가 직접 봄.
- 검증 결과: v1(약함)→v3(외곽선 굿, 눈 색 뜸)→v4(눈 파랑 수정). 파이프라인 완전 작동. 머티리얼이 같은 T__* 에셋 참조 → 재임포트 즉시 반영, 리그 0변경.

## 🔀 원본↔스타일 토글 (`toggle_test7.py`)
에디터 콘솔에서 `py "C:/Secret_Project/toggle_test7.py"` → 6개 텍스처를 원본(`test7_tex`)↔v4(`test7_tex_styled4`) 재임포트로 전환(~10~15s, 뷰포트 즉시 갱신, 비파괴). 상태파일 `_tripo/_toggle_state.txt`. 머티리얼 매핑: T__04=Face/T__02=EyeIris/T__10=Body/T__12=HairBack/T__13=Tops/T__17=Tops, 베이스색 파라미터=gltf_tex_diffuse·mtoon_tex_MainTex·Shade·ShadeTexture. (※머리front=T__18, 입=T__01 등은 미교체 — 필요시 그리드에 추가.)

## 🔁 워크플로우 규칙
- 캡처 후 **반드시 사용자 검수** (B 방식: "찍은 것 검수받아"). 빗나간 부위만 본좌표로 재촬영해 덮어쓰기.
- 캐릭터당: 프로브 4장으로 FRONT_YAW 확정 → full 37장 → 검수 → 스타일변환 → 텍스처 교체.
